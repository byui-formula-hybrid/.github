# Example caller workflow: call-discord-pr.yml
# This workflow runs in the caller repo and invokes the reusable PR notification workflow

name: Call Discord PR Notification

on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  notify:
    uses: byui-formula-hybrid/.github/.github/workflows/discord-pr-notification.yml@main
    secrets:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      DISCORD_ROLE_ID: ${{ secrets.DISCORD_ROLE_ID }}
